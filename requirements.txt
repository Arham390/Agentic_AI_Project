# ── Core LLM & orchestration ────────────────────────────────────────────────
python-dotenv
langgraph
langchain
langchain-core
langchain-openai
langchain-groq
pydantic>=2

# ── Phase 4: Web interface ───────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── MCP server (Phase 1 diagram requires an MCP tool surface) ───────────────
mcp

# ── Memory backend (falls back to JSON file store if missing) ────────────────
chromadb

# ── Phase 2: Image generation (optional — only needed for Local SD) ──────────
# CPU-only PyTorch (no NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
diffusers
transformers
accelerate
safetensors
pillow

# ── Phase 2 & 3: Audio + video assembly ──────────────────────────────────────
numpy
imageio
imageio-ffmpeg
pyttsx3
edge-tts
opencv-python

# ── Testing ───────────────────────────────────────────────────────────────────
pytest
httpx         
